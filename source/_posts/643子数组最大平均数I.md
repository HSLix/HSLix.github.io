---
title: 【Leetcode Daily】643子数组最大平均数I
author: 陆爻齐-LuYaoQi
cover: /img/index/leetcode.png
tags:
  - Algorithm
  - 滑动窗口
  - 定长滑动窗口
categories:
  - 力扣日常 | LeetcodeDaily
series:
  - 力扣日常 | LeetcodeDaily
hide: true
abbrlink: b41dda00
date: 2025-04-17 10:29:12
---

# 题目浅析
- 想查看原题可以点击[题目链接](https://leetcode.cn/problems/maximum-average-subarray-i/)。

- 简单地说，就是给一个整数数组和数字 k，要求给出其中 k 个长度的连续子数组平均数的最大值。

# 思路分享
- 自从学了定长滑动窗口三步法，思路就很清晰。本题窗口固定长度，可以用。1. 入；2. 更新；3. 出；具体分析请看 {% post_link 1456定长子串中元音的最大数目 %}。

# 代码解答（强烈建议自行解答后再看）
- 滑动窗口
```c++
class Solution {
public:
    double findMaxAverage(vector<int>& nums, int k) {
        double max_average = -(numeric_limits<double>::max());
        double sum = 0;

        for (int i = 0; i < nums.size(); i++) {
            sum += nums[i];

            if (i < k - 1) continue;

            if ((sum / k) > max_average) {
                max_average = sum / k;
            }

            sum -= nums[i-k+1];
        }
        return max_average;
    }
};
```
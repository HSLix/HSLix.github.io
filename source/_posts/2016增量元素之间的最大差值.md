---
title: 【Leetcode Daily】2016增量元素之间的最大差值
author: 陆爻齐-LuYaoQi
cover: /img/index/leetcode.png
tags:
  - Algorithm
  - 枚举
categories:
  - 力扣日常 | LeetcodeDaily
series:
  - 力扣日常 | LeetcodeDaily
hide: true
abbrlink: cf91cc6f
date: 2025-06-16 15:49:22
---

# 题目浅析
- 想查看原题可以点击[题目链接](https://leetcode.cn/problems/maximum-difference-between-increasing-elements/description/)。

- 简单地说，就是给一个整型数组，求其中两个数的最大差值，要求是后一个数减去前一个数的差值，且两个数的下标不能相同。

# 思路分享
- 就是普通枚举，而且只要保存一个数的下标左侧数值的最小值，然后与当前枚举数字做差值，取最大即可。

- 整体思路与 {% post_link 121买卖股票的最佳时机-枚举 %} 的思路一致。

- O(n) 枚举的基本思想可以看 {% post_link 1两数之和 %}。

# 代码解答（强烈建议自行解答后再看）
- 参考题解
```c++
class Solution {
public:
    int maximumDifference(vector<int>& nums) {
        int res = -1;
        int min_num = nums[0];
        for (const int i : nums) {
            if (i > min_num) {
                res = max(res, i-min_num);
            }
            min_num = min(min_num, i);
            //cout << min_num << endl;
        }
        return res;
    }
};
```
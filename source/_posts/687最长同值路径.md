---
title: 【Leetcode Daily】687最长同值路径
author: 陆爻齐-LuYaoQi
cover: /img/index/leetcode.png
tags:
  - Algorithm
  - 二叉树
categories:
  - 力扣日常 | LeetcodeDaily
series:
  - 力扣日常 | LeetcodeDaily
hide: true
abbrlink: 4657e79b
date: 2026-02-06 15:19:08
---

# 题目浅析
- 想查看原题可以点击[题目链接](https://leetcode.cn/problems/longest-univalue-path/description/)。

- 简单地说，就是给一个二叉树，现在要在其中找到一条最长的路径，路径中要求值相同。

# 思路分享
- 和 {% post_link 543二叉树的直径 %} 差不多，不过要注意数值的传递，毕竟本题要求路径数值相同，所以如果点的数值和子树不同，那就说明下面断了，直接传 0。
> https://leetcode.cn/problems/longest-univalue-path/solutions/2227160/shi-pin-che-di-zhang-wo-zhi-jing-dpcong-524j4/

# 代码解答（强烈建议自行解答后再看）
- 参考题解
```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def longestUnivaluePath(self, root: Optional[TreeNode]) -> int:
        ans = 0
        def dfs(root: Optional[TreeNode]) -> int:
            if not root:
                return -1 
            left = dfs(root.left) + 1
            right = dfs(root.right) + 1
            
            if root.left and root.val != root.left.val:
                left = 0
            if root.right and root.val != root.right.val:
                right = 0

            cur = left + right
            nonlocal ans 
            if ans < cur: 
                ans = cur 
            
            return max(left, right)
        
        dfs(root)
        return ans
```
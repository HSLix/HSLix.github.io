---
title: 【Leetcode Daily】234回文链表
author: 陆爻齐-LuYaoQi
cover: /img/index/leetcode.png
tags:
  - Algorithm
  - 链表
categories:
  - 力扣日常 | LeetcodeDaily
series:
  - 力扣日常 | LeetcodeDaily
hide: true
abbrlink: 9eaf2c3e
date: 2026-01-09 10:22:11
---

# 题目浅析
- 想查看原题可以点击[题目链接](https://leetcode.cn/problems/palindrome-linked-list/description/)。

- 简单地说，就是规定回文链表就是向前和向后读都相同序列的链表，求判断当前链表是否是回文链表。

# 思路分享
- 本题为“拼好题”，顾名思义，需要懂得几个基本题型才比较好解决。

- 向前向后序列相同，可以进一步变成后半段反过来就是前半段，所以先通过 {% post_link 876链表的中间结点 %} 找到中间结点，再通过 {% post_link 206反转链表 %} 将后半段反转接着就可以对前后半段直接一个个比较值是否相同，于是就能时间复杂度 O(n)，空间复杂度 O(1)
> https://leetcode.cn/problems/palindrome-linked-list/solutions/2952645/o1-kong-jian-zuo-fa-xun-zhao-zhong-jian-rv0f3/

# 代码解答（强烈建议自行解答后再看）
- 参考题解
```python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def get_mid(self, head):
        left, right = head, head
        while right and right.next:
            left, right = left.next, right.next.next
        return left
    
    def reserve_list(self, head):
        pre = None
        cur = head
        while cur:
            nxt = cur.next
            cur.next = pre
            pre = cur
            cur = nxt
        return pre
    
    def isPalindrome(self, head: Optional[ListNode]) -> bool:
        mid = self.get_mid(head)
        mid = self.reserve_list(mid)
        while mid:
            if head.val == mid.val:
                # print(head, mid) # 虽然前一段的最后一个指针没有编辑，但不妨碍答案的检测
                head, mid = head.next, mid.next
            else:
                return False
        return True
```
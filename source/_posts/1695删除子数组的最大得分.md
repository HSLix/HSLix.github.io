---
title: 【Leetcode Daily】1695删除子数组的最大得分
author: 陆爻齐-LuYaoQi
cover: /img/index/leetcode.png
tags:
  - Algorithm
  - 滑动窗口
  - 不定长滑动窗口
categories:
  - 力扣日常 | LeetcodeDaily
series:
  - 力扣日常 | LeetcodeDaily
hide: true
abbrlink: b5561483
date: 2025-05-07 09:52:45
---

# 题目浅析
- 想查看原题可以点击[题目链接](https://leetcode.cn/problems/maximum-erasure-value/description/)。

- 简单地说，就是求数组中一段子数组的和的最大值，要求子数组内的元素不重复。

# 思路分享
- 纯粹的不定长滑动窗口（{% post_link 3090每个字符最多出现两次的最长字符串 %}），唯一需要注意的是，记得左侧滑动窗口时，减去最左元素的值。

# 代码解答（强烈建议自行解答后再看）
- 陆爻齐的解法
```c++
class Solution {
public:
    int maximumUniqueSubarray(vector<int>& nums) {
        int sum = 0;
        int ans = 0;
        int left = 0;
        unordered_map<int, int>rec;
        int n = nums.size();
        for (int i = 0; i < n; i++) {
            int m = nums[i];
            sum += m;
            rec[m]++;
            while(rec[m]>1) {
                sum -= nums[left];
                rec[nums[left++]]--;
            }
            ans = max(sum, ans);
        }
        return ans;
    }
};
```
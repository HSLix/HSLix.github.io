---
title: 【Leetcode Daily】110平衡二叉树
author: 陆爻齐-LuYaoQi
cover: /img/index/leetcode.png
tags:
  - Algorithm
  - 二叉树
categories:
  - 力扣日常 | LeetcodeDaily
series:
  - 力扣日常 | LeetcodeDaily
hide: true
abbrlink: 40177f93
date: 2026-01-29 10:03:20
---

# 题目浅析
- 想查看原题可以点击[题目链接](https://leetcode.cn/problems/balanced-binary-tree/description/)。

- 简单地说，就是给一个二叉树，判断这是不是平衡二叉树（任意节点左右子节点高度差不超过 1）

# 思路分享
- 通过递归，可以每个节点都判断子节点的高度情况。灵神这里在非法情况下通过 -1 这个不可能高度值来表示，更加简便。
> https://leetcode.cn/problems/balanced-binary-tree/solutions/2015068/ru-he-ling-huo-yun-yong-di-gui-lai-kan-s-c3wj/

# 代码解答（强烈建议自行解答后再看）
- 参考题解
```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def isBalanced(self, root: Optional[TreeNode]) -> bool:
        if not root:
            return True
        ans = True
        def checkHeight(root:Optional[TreeNode]) -> int:
            if not root:
                return 0
            left = checkHeight(root.left)
            right = checkHeight(root.right)
            if abs(left - right) > 1:
                nonlocal ans 
                ans = False
            return max(left,right) + 1
        checkHeight(root)
        return ans
```
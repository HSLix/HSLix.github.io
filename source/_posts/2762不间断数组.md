---
title: 【Leetcode Daily】2762不间断数组
author: 陆爻齐-LuYaoQi
cover: /img/index/leetcode.png
tags:
  - Algorithm
  - 滑动窗口
  - 不定长滑动窗口
categories:
  - 力扣日常 | LeetcodeDaily
series:
  - 力扣日常 | LeetcodeDaily
hide: true
abbrlink: 23cfa8ee
date: 2025-05-27 10:14:12
---

# 题目浅析
- 想查看原题可以点击[题目链接](https://leetcode.cn/problems/continuous-subarrays/description/)。

- 简单地说，就是给一个整数数组，并找出其中所有子数组的数目，子数组的要求为其中任意两元素之差不大于 2.

# 思路分享
- 越短越合法的不定长滑动窗口的简单变式（{% post_link 713乘积小于K的子数组 %}

- 暴力一点的思路，是每次窗口变动，都遍历一边窗口内的元素找其中最大值与最小值之差，但是这样的话，复杂度就是 O(n^2)，所以解决这个问题就是本题关键。

- 以 C++ 的角度，无论是 map 还是 multiset 都能实现本题，陆爻齐采用 map 的方式，可以 O(1) 地取到窗口内的最大值和最小值，只要对首尾迭代器取 first 就行，毕竟这里看的是值差，并非数目差。
> https://leetcode.cn/problems/continuous-subarrays/solutions/2327219/shuang-zhi-zhen-ping-heng-shu-ha-xi-biao-4frl/

# 代码解答（强烈建议自行解答后再看）
- 参考题解
```c++
class Solution {
public:
    long long continuousSubarrays(vector<int>& nums) {
        map<int, int> rec;
        int left = 0;
        long long ans = 0;
        for (int right = 0; right < nums.size(); right++) {
            rec[nums[right]]++;
            while(!rec.empty() && rec.rbegin()->first-rec.begin()->first > 2) {
                int index = nums[left++];
                rec[index]--;
                if (rec[index] == 0) {
                    rec.erase(index);
                }
            }
            ans += right-left+1;
        }
        return ans;
    }
};
```
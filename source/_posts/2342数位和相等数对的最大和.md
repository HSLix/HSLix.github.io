---
title: 【Leetcode Daily】2342数位和相等数对的最大和
author: 陆爻齐-LuYaoQi
cover: /img/index/leetcode.png
tags:
  - Algorithm
  - 枚举
categories:
  - 力扣日常 | LeetcodeDaily
series:
  - 力扣日常 | LeetcodeDaily
hide: true
abbrlink: 5d1c0b81
date: 2025-06-18 10:05:47
---

# 题目浅析
- 想查看原题可以点击[题目链接](https://leetcode.cn/problems/max-sum-of-a-pair-with-equal-sum-of-digits/description/)。

- 简单地说，就是给一个整型数组，找其中数位和相同的两两一组的数字，从中找每组数组和最大值。

# 思路分享
- 与 {% post_link 2815数组中的最大数对和 %} 几乎相同，只是把每组数字的要求变化为数位和相同，故不多赘述。

- O(n) 枚举的基本思想可以看 {% post_link 1两数之和 %}。

- 灵神看出本题的数位和不超过 10 位数字，故用长度 82 的数组当哈希表使用，确实能够提升效率。
> https://leetcode.cn/problems/max-sum-of-a-pair-with-equal-sum-of-digits/solutions/2531487/mei-ju-you-wei-hu-zuo-pythonjavacgojsrus-eoys/

# 代码解答（强烈建议自行解答后再看）
- 参考题解
```c++
class Solution {
public:
    int maximumSum(vector<int>& nums) {
        int max_sum = -1;
        unordered_map<int, int> rec; // 数位和 对 最大数值
        for (const int &i : nums) {
            int place_sum = 0;
            for (int val = i; val > 0; val /= 10) {
                place_sum += val % 10;
            }
            if (rec.find(place_sum) != rec.end()) {
                max_sum = max(max_sum, rec[place_sum]+i);
            }
            rec[place_sum] = max(rec[place_sum], i);
        }
        return max_sum;
    }
};
```